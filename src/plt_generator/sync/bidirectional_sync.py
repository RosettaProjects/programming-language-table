from ..data import FeatView, LangView


def sync_feat_to_view(
    featview: FeatView, langview: LangView
) -> tuple[FeatView, LangView]:
    return featview, langview


def sync_view_to_feat(
    featview: FeatView, langview: LangView
) -> tuple[FeatView, LangView]:
    return featview, langview


def sync_bidirectional(
    featview: FeatView, langview: LangView
) -> tuple[FeatView, LangView]:
    return featview, langview
